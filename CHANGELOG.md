# 更新日志

## 2025-11-14 — 魔改 v1.6（成就持久化 / 隐藏天赋描述去括号）

### 新增
- **成就持久化（跨局累计）**：成就记录不再只跟着存档走，而是单独存一份在
  `localStorage['oi_achievements_profile_v1']` 里 —— **换存档、重开一局、清档都不会丢**。
  - 档案结构：`{ version, runs, unlocked: { '<id>': { first, last, runs } } }`，记录首次 / 最近解锁时间与解锁过的局数。
  - 判定口径：已解锁 = **本局记录 ∪ 跨局档案**。所以面板、顶栏徽章、重复提示都以跨局为准，
    已经拿到的成就不会在下一局重新弹一次提示。
  - 面板头部改成「🏅 成就（跨局累计）x / 93」，并显示「本局新解锁 N 个 · 累计进行 M 局」。
  - 面板底部新增 **清空成就记录** 按钮（二次确认），会同时清掉跨局档案与本局记录，方便重新体验。
  - 老存档读档时会自动 `syncProfileFromRun()` 把里面已解锁的成就补进档案，不会因为读一次档就退回去。
  - `localStorage` 不可用（隐私模式 / 配额满）时自动降级成内存档案，不写盘也不会报错。

### 调整
- **隐藏天赋描述清理括号**：
  - `世界上最幸福的女孩`：`我永远喜欢珂朵莉。（每打完一场正式比赛……不剧透）` → `我永远喜欢珂朵莉。`
  - `样例过了就是过了`：`样例过了就是过了。（至于评测机认不认，那是另一回事）` → `样例过了就是过了。`
  - `你怎么知道我 AK 了`：`你怎么知道我 AK 了？（我可没说，是你自己点开天赋栏看的）` → `你怎么知道我 AK 了？`
  - 唯一保留括号的是 `嬲选手` 的 `（你可以去源代码查看功能）`。

### 文档
- `README.md` / `AGENTS.md` 同步了成就持久化说明与新增的存档位置。

## 2025-11-14 — 魔改 v1.5（外显成就 + 隐藏天赋名称去后缀）

### 新增
- **外显成就（38 个）**：成就系统拆成两类 —— 「🏅 成就」是**明牌目标**，锁着的时候也直接显示名字、条件与进度条，
  照着做就行（首次训练、打进 NOIP、站上 NOI、经费 25 万、设施起步……）；
  「❓ 隐藏成就」（沿用原来的 55 个）继续不剧透。
  成就面板顶部加了**两栏 Tab**，默认停在外显成就一栏，顶栏徽章显示两者合计（`已解锁 / 93`）。

### 调整
- **隐藏天赋不再加「(隐藏)」后缀**：出境集训的「天赋激发」列表里，天赋直接显示本名。
  隐藏天赋仍然保留虚紫边框作为区分。

### 文档
- `README.md` / `AGENTS.md` 同步了成就分类与数量；`shared.html` 的成就数统计包含两类。

## 2025-11-14 — 魔改 v1.4（随机数一致化 / 成就扩充 / 仓库说明书）

### 新增
- **随机数一致化（同种子 = 同结果）**：
  - 所有影响游戏进程的随机数统一走 `getRandom()`（可种子化随机源），
    清掉了散落在 `game.js` / `render.js` / `lib/countries.js` / `lib/talent.js` / `lib/contest-integration.js`
    等处的 `Math.random()`。
  - 随机源换成 **sfc32 + xmur3 播种**（`lib/utils.js` 的 `SeededRandom`）：同一个种子必然产出同一串数字，
    分布也比原来的 xorshift 实现更均匀。
  - **随机种子与随机流会写进存档**（`game.randomSeed` / `game.rngState`），
    存读档、刷新页面之后随机流会接回原处，所以「同样的操作」依然是「同样的结果」。
  - 普通开局现在也有种子（`generateRandomSeed()`），只是每局不同；
    赛季结算页会显示本局种子，可以分享给别人复现。
  - 纯装饰性随机（随机一言、DOM id、矩阵雨背景）改用 `getCosmeticRandom()`，不消耗种子流。
  - 新增工具：`getCosmeticRandom()` / `withRandomSeed()` / `pickRandom()` / `weightedPick()` /
    `getRandomState()` / `restoreRandomState()` / `generateRandomSeed()` / `describeRandomSeed()`。
- **今日挑战重做**：种子改为由日期直接派生（`getDailyChallengeParams()` 不再用 `Math.sin` 近似），
  同一天所有玩家的省份与种子完全一致，且与「什么时候点按钮」无关。
- **隐藏成就从 17 个扩充到 55 个**：按「出境集训 / 队伍与教练 / 天赋 / 比赛 / 经营 / 心态 / 彩蛋」七类归档，
  面板加了分类胶囊、分组标题和**未解锁进度条**（一眼看出还差多少）。
  新增统计埋点：训练 / 加训 / 打工 / 娱乐 / 模拟赛 / 集训次数、天赋获得与失去、经费与声誉极值、
  压力峰值、生病次数、劝退与挽救次数、比赛场次、满分场次、奖牌数、头名次数等。
- **`AGENTS.md`**：仓库说明书 —— 每个文件是干什么的、随机数与「学生姓名是身份主键」两条铁律、
  常见改动指引、存档兼容注意事项、提交与推送约定。

### 修复
- **开局自动存档会把天赋洗掉**：`?new=1` 开局后原本直接 `JSON.stringify(game)` 落盘，
  而 `JSON.stringify` 会把天赋的 `Set` 变成 `{}`，刷新一次页面所有天赋都没了。现在改走 `saveGame()`。
- **读档报 `game.completedCompetitions.has is not a function`**：JSON 没有 `Set` 类型，
  读档时没有还原，导致渲染学生晋级状态时每次都抛错。新增 `__restoreSetFields()`
  统一还原 `completedCompetitions`、`qualification`、学生 `talents`（顺序上有讲究，见 `AGENTS.md` 第 6 节）。
- **同种子重开一局题目会变**：`lib/task.js` 里「最近推荐过的题目」是跨局共享的全局缓存，
  现在用 `game.gameToken` 按局隔离。

### 文档
- `README.md` 更新了隐藏成就数量与随机种子说明；`shared.html` / 分享数据里带上了随机种子与成就。

## 2025-11-14 — 魔改 v1.3（退队保护）

### 新增
- **退队保护**：压力顶到退队线（90）时，学生**不再当场退队**，而是先产生「退队倾向」并推一张警告卡，
  给玩家一周缓冲：这一周里把压力拉回 90 以下就能化解（放假、娱乐、降低训练强度都行），
  缓冲期结束后压力仍然顶着才会真的退队。
  - 压力继续顶在 100 及以上：缓冲用完后退队是必然的（「乐天派」还有一半机会再躲一次）。
  - 压力落在 90~99：缓冲用完后再按原概率退队（「乐天派」减半）。
  - **加训 / 打工**这两条"压力拉满"的路径同样受保护，不再一点确认就直接把人送走。
  - 判定逻辑集中在 `game.js` 的 `evaluateQuitRisk()`，阈值见 `lib/constants.js` 的
    `QUIT_PRESSURE_TENDENCY` / `QUIT_PRESSURE_HARD` / `QUIT_PROTECT_WEEKS`。
- 学生卡片上的「退队倾向」标签加了 tooltip，说明保护期怎么算。
- 帮助文档同步补充了退队保护的说明。

### 修复
- 加训退队的事件卡文案写着「声誉-10」但实际从未扣声誉，现在文案与实际行为一致。
- 压力退队（burnout 事件）此前没有累计 `quit_students` 统计，现在会正确累加。

## 2025-11-14 — 魔改 v1.2（隐藏天赋玩梗化 / 国家事件重做 / 隐藏成就）

### 新增
- **隐藏成就系统**：新增 `lib/achievements.js`，一局游戏里会偷偷记录 17 个彩蛋成就。
  解锁前只显示「？？？」和一句提示，解锁后亮出真面目；游戏中点右上角 **🏅 隐藏成就** 查看，赛季结算页也会列出。
- **隐藏天赋改名 + 重写描述**：6 个魔改隐藏天赋改成玩梗命名（`样例过了就是过了`、`暴力出奇迹`、
  `你怎么知道我 AK 了`、`稳如老狗`、`Deadline 是第一生产力`、`欧皇`），效果描述统一改成
  「点到为止」的愚人节彩蛋风格（不再直接写出具体数值）。
- **天赋标签全面渐变**：所有天赋标签（含天赋卡片上的色点）改由 `TalentManager.getTalentTagStyle()` 统一生成，
  自动推导渐变配色；`珂朵莉`（红蓝）、`世界上最幸福的女孩`、`嬲选手`（彩虹）等有专属多色渐变。
- **国家 / 地区搜索**：`country.html` 与出境集训弹窗都支持搜索（中文名、拼音首字母、英文名、关键词）。
- **网梗彩蛋**：随机一言与部分事件文案里补充了一批 OI / 网梗，尽量自然，不硬塞。
- **超级学生更新**：`李欣隆` 现在自带全部隐藏天赋；`wzl` 保留女队身份；训话框输入彩蛋口令可召唤 `珂朵莉`。

### 修复
- **出境集训的国家事件改为「按学生」触发**：此前无论队伍里有几个人，每次集训都只有固定的一名学生
  触发国家事件；现在每名参加集训的学生各自独立掷骰子（概率见 `CHUJINGFAZHI`），可能多人分别遇到不同的事，
  也可能一个人都没碰上。
- **走上女队道路不再"换人"**：此前会直接把学生改名为「XXX（女队）」，而晋级资格、比赛履历都是**按名字索引**的，
  改名后这名学生的通过记录会全部对不上（表现成"换了一个人"）。现在只打身份标记，名字不动，
  UI 上以「女队」标签呈现；读档时还会自动修复旧存档里被改过名的学生并找回履历。
- 出境集训的**国家费用倍率此前完全没生效**：算钱时误用了同 id 的国内省份类型，导致选哪个国家价格都一样。
  现在 `computeOutingCostQuadratic` 支持国家费用倍率，弹窗里的"预计费用"也与实际扣款一致。
- **经费不足导致集训 / 出境集训失败时，行动值照样消耗**（推进一周）；若当下还有未处理的事件卡片，
  这笔账会记下来，等下一次真正推进周数时一并补扣，避免"看一眼价格就白嫖"。
- 隐藏天赋旧名字在旧存档中会被自动改写为新名字，读档不会丢天赋。

### 平衡
- **出境集训不再是纯正面**：每次出境集训都有概率碰上意外事件（共 17 条，其中 3 条为特定国家 / 地区专属），
  效果以负面为主（掉钱、生病、压力、心理、舒适度、知识点），且有较低概率一次碰上两起。
- 在**香港 / 澳门**开局时，送学生去**港澳**集训不收路费。

### 文档 / 命名
- 规范名称：所有入口由「查看国家」改为「**国家 / 地区**」。
- 更新了国家事件与国家 / 地区一览的内容（具体效果与事件不在文档里剧透）。

## 2025-11-13 — 魔改 v1.1.1（同步上游重写的 UI）

### 修复
- **修复「点击开始游戏没反应」**：合并上游时，`start.html` 里魔改版自己加的启动蒙版（`#oiStartMask`）被保留，
  但负责隐藏它的 `<script>` 在合并中被静默丢弃，导致蒙版永远盖在整个页面上，点什么都没用。
  现在**直接移除该蒙版，`start.html` 完全采用上游重写后的开始界面**（三步向导 + 可交互省份地图）。
- 顺手删掉 `styles.css` 中随之失效的 `.oi-start-mask` / `.oi-start-subtitle` 样式（共 57 行）。
- 补回 `end.html` 漏掉的「再玩一次」按钮（`#end-playagain`）：合并时上游的按钮行被魔改版覆盖，
  但上游的处理脚本被保留下来，成了悬空引用。现在按钮行为「回到开始 / 分享结果 / 再玩一次 / 查看国家 / 攻略」。

### 说明
- `index.html` / `shared.html` / `game.html` 的界面已与上游一致，本分支只额外保留：署名、
  🌍 查看国家 / 📖 攻略 入口，以及加训 / 打工 / 出境集训三个行动卡。

## 2025-11-13 — 魔改 v1.1（同步上游 + 内容补全）

### 同步上游
- 手工合并 `seve42/OItrainer:main`（43 个提交），并保留本分支的魔改功能。
- 带入上游的重构：设施系统、气候系统（`lib/climate.js`）、省份模块（`lib/provinces.js`）、中文简繁转换、重名修复、比赛计算结构调整等。

### 新增
- 🌍 **查看国家**：新增 `country.html`，列出全部 56 个出境集训国家 / 地区的大致印象（点到为止，不剧透具体事件），并在游戏顶栏与出境集训弹窗里加了入口。
- **出境集训各国专属效果**：每个国家 / 地区现在都有各自不同的集训彩蛋（此前只实现了 6 个，其余十几个被注释掉、剩下的完全没有效果）。少数国家还能直接觉醒隐藏天赋。
- **6 个新的强力隐藏天赋**：`蒙的全对`、`算法之神`、`AK机器`、`心如止水`、`时之沙漏`、`天命之子`。

### 数值平衡 / 难度上调
- 训练收益 ×0.92、集训收益 ×0.95、压力增长 ×1.15、分数线 ×1.08、题目难度 ×1.06、经费消耗 ×1.10（见 `lib/constants.js` 的「全局增幅变量」）。
- `DIFFICULTY_MULTIPLIER` 现在真正作用于比赛题目难度（此前只影响训练题）。
- 天赋获取总概率 0.15 → 0.12。

### 修复
- `getKnowledgeByType` 之前忽略比赛中的临时知识点加成，导致「伽罗瓦 / 爆发型 / 珂朵莉」等天赋的知识点效果被静默丢弃。
- 「数据结构狂热者 / 图论直觉 / 赛场狂热 / 最后一搏 / 跳跃思维 / 偏科」原本写死 `contestName === '模拟赛'`，而模拟赛的实际名称是 `CF Div.2` 之类，导致这 6 个天赋从不触发。
- 「专注」「厌学」原本只认 `source === 'training'`，实际事件源是 `task_training`，导致从不触发。
- 「追风者」的 `typhoon` 事件源无人发送，台风事件现在会正确触发它。
- 「举一反三」「好奇宝宝」会在压力预览阶段提前结算收益（等于白嫖两次），现在预览模式下不再产生副作用。
- 「摸鱼大师」承诺的「知识增益 -30%」、「厌学」承诺的「训练无效」此前无人执行，现在会在训练结算时真正生效。
- 「高原反应」「水土不服」现在对出境集训同样生效。
- 出境集训的 `CHUJINGFAZHI` 原本写成 1.5（概率 >1），改为 1.0 并注明语义。
- 马拉西亚的 `bonus` 里误写了一个 `pressureFactor`；恢复页多余的 `</div>`；`start.html` 启动蒙版里错误的 `</h3>`。

### 精简
- 删除无用文件：`script.js.del`、`lib/competitions.js.backup`、`API_SUMMARY.md`（自述已过期）、`events_adden`（草稿）、`demo-choice.html`。
- 上游已删除的 `test-*.html` / `talent-demo.html` / `share-example.html` 等一并移除。
- 不再跟踪 Visual Studio 的 `.vs/` 目录，新增 `.gitignore`。
- 出境集训内联在 `game.js` 里的 369 行 `switch` 改为数据驱动的 `lib/countries.js`。

## 2026-07-03 - v0.6.0.1（上游）
为省份特性预留接口
优化开始菜单动画
优化设施升级页面

## 2026-07-01 — v0.6.0（上游）
更新了开始页面与菜单
优化了设施升级页面的渲染逻辑
